<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <EditText
            android:id="@+id/find_city"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="@string/city_name_hint"
            android:singleLine="true"
            android:textAppearance="@style/TextAppearance.AppCompat.Title" />
        <ImageButton
            android:id="@+id/get_weather_by_location_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignEnd="@id/find_city"
            android:background="#00FFFFFF"
            android:paddingTop="12dp"
            android:paddingEnd="6dp"
            android:src="@drawable/ic_location_on_black_24dp"
            />
        <Button
            android:id="@+id/get_weather_by_city_button"
            android:padding="@dimen/activity_vertical_margin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimary"
            android:textColor="@color/cardview_light_background"
            android:drawableStart="@drawable/ic_find_replace_white_24dp"
            android:layout_centerHorizontal="true"
            android:layout_below="@id/find_city"
            android:text="@string/get_weather"
            />
    <include layout="@layout/weather_details"/>
    </RelativeLayout>
</ScrollView>

